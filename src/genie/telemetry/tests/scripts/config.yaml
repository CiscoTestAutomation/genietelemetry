plugins:
    crashdumps:
        interval: 30
        module: genie.libs.telemetry.plugins.crashdumps
    tracebackcheck:
        interval: 30
        module: genie.libs.telemetry.plugins.tracebackcheck
    mockplugin:
        interval: 10
        module: genie.telemetry.tests.scripts.mockplugin